spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgres://localhost:5432/customers
    username: admin
    password: customers@admin

  jpa:
    hibernate:
      ddl-auto: update
  show-sql: true

  sql:
   init:
     platform: postgres