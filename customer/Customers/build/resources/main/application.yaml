spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/customers
    username: admin
    password: customers@admin

jpa:
  hibernate:
    dialect: postgres
    ddl-auto: update
  show-sql: true

  sql:
   init:
     platform: postgres